"""
Калькулятор длины окружности
----------------------------
Программа запрашивает у пользователя радиус окружности,
проверяет корректность ввода и вычисляет длину окружности.
"""

from math import pi


def get_radius():
    """
    Запрашивает у пользователя радиус окружности.

    Пользователь вводит число. Программа:
    - Проверяет, что ввод — положительное число (целое или дробное)
    - При ошибке — просит ввести снова

    Returns:
        float: положительное число — радиус окружности
    """
    while True:
        radius_input = input("Введите радиус окружности: ").strip()

        try:
            radius = float(radius_input)
            if radius <= 0:
                print("Ошибка: радиус должен быть больше нуля!")
                continue
            return radius
        except ValueError:
            print("Ошибка: введите корректное число (например, 5 или 3.14)!")


def calculate_circumference(radius):
    """
    Вычисляет длину окружности по формуле C = 2πr.

    Args:
        radius (float): радиус окружности

    Returns:
        float: длина окружности, округлённая до 2 знаков после запятой
    """
    return round(2 * pi * radius, 2)


# Основная часть программы
if __name__ == "__main__":
    """
    Точка входа в программу.
    Запрашивает радиус, вычисляет длину окружности и выводит результат.
    """
    print("📏 Калькулятор длины окружности")
    print("Введите радиус, и я посчитаю длину окружности.\n")

    radius = get_radius()
    result = calculate_circumference(radius)

    print(f"\n✅ Длина окружности: {result} см")