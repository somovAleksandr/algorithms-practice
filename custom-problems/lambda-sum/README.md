# Lambda Sum - Вычисление суммы трёх чисел через вложенные лямбды

Проект демонстрирует использование **вложенных лямбда-выражений** в Python для создания функции, которая принимает три аргумента поочерёдно:

```python
sum3(2)(4)(6)  # → 12
```
