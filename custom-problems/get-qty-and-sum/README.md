# Подсчёт чётных и сумма нечётных чисел

Простой Python-скрипт, который подсчитывает количество чётных чисел и сумму нечётных чисел в заданном списке.

---

## 📌 Описание

Функция `get_qty_and_sum(nums)` принимает список целых чисел и возвращает:

- Количество **чётных** чисел
- Сумму **нечётных** чисел

Пример использования:

```python
qty_even, sum_odd = get_qty_and_sum([21, 22, 23, ..., 40])
print(qty_even)  # → 10
print(sum_odd)   # → 300
```
